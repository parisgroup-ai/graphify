# GitHub Issues 9 and 10 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix TypeScript alias resolution for nearest `tsconfig.json` lookup and align Go extraction with package-scoped module IDs.

**Architecture:** Extend resolver context for TypeScript aliases so resolution can depend on the source module, then switch Go discovery to package-level module IDs so import resolution and discovered nodes use the same identity shape. Keep the fix local to walker, resolver, and pipeline assembly.

**Tech Stack:** Rust workspace, cargo test, graphify-extract, graphify-cli.

---

## Chunk 1: TypeScript alias context

### Task 1: Add failing tests for nearest-tsconfig alias lookup

**Files:**
- Modify: `crates/graphify-extract/src/resolver.rs`

- [ ] **Step 1: Write the failing test**
- [ ] **Step 2: Run the resolver tests to verify the new test fails**
- [ ] **Step 3: Implement per-module tsconfig alias context support**
- [ ] **Step 4: Run the resolver tests to verify they pass**

### Task 2: Wire tsconfig discovery from the CLI pipeline

**Files:**
- Modify: `crates/graphify-cli/src/main.rs`

- [ ] **Step 1: Write the failing integration-style test if coverage is missing**
- [ ] **Step 2: Run the focused test to verify it fails**
- [ ] **Step 3: Load nearest tsconfig aliases for discovered TypeScript modules**
- [ ] **Step 4: Run the focused tests to verify they pass**

## Chunk 2: Go package identity

### Task 3: Add failing tests for package-scoped Go discovery and resolution

**Files:**
- Modify: `crates/graphify-extract/src/walker.rs`
- Modify: `crates/graphify-extract/src/resolver.rs`

- [ ] **Step 1: Write failing Go discovery and resolver tests**
- [ ] **Step 2: Run the focused tests to verify they fail**
- [ ] **Step 3: Implement package-scoped Go module naming**
- [ ] **Step 4: Run the focused tests to verify they pass**

### Task 4: Regression verification

**Files:**
- Modify: `crates/graphify-extract/src/go.rs` if test fixtures need package IDs

- [ ] **Step 1: Run targeted crate tests**
- [ ] **Step 2: Run workspace formatting**
- [ ] **Step 3: Run broader workspace tests if the targeted suite is green**
